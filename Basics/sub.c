// to find difference of two numbers
#include <stdio.h>
int main()
{
    int a, b, s;
    printf("enter number ");
    scanf("%d %d", &a, &b);
    s = a - b;
    printf("the subtraction is %d", s);
    return 0;
}